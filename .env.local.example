# AWS SES SMTP Configuration
SES_SMTP_USERNAME=your-ses-smtp-username
SES_SMTP_PASSWORD=your-ses-smtp-password

# SMTP Settings
SMTP_ADMIN_EMAIL=noreply@yourdomain.com  # Must be verified in AWS SES
SMTP_HOST=email-smtp.ap-northeast-1.amazonaws.com
SMTP_PORT=587
SMTP_SENDER_NAME=SEIKOU Contact Form

# Important for AWS SES:
# 1. The SMTP_ADMIN_EMAIL must be verified in your AWS SES account
# 2. If in sandbox mode, recipient emails must also be verified
# 3. To send to any email, request production access in AWS SES